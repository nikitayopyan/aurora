Здравствуйте! Меня зовут Никита и это моё мини-описание!
=================================================================

Структура html кода написана по методологии БЭМ (частично)

Препроцессор CSS использовал SCSS (для перевода в css не использовал npm/gulp, а плагин в VSкоде с кастомными настройками из документации)
Распределил 3 секции по отдельным scss-файлам в папке blocks и импортировал в основной файл style.scss
media-запросы написал в отдельном файле и папке base под разрешение от 320px до 480px, которые тоже импортировал, внизу основного файла style.scss 

JS файл только один поскольку кода не слишком много, каждая часть которого прокомментирована  

(в самом низу есть функция которая срабатывает в интервале каждые 35 секунд, она меняет url бэкграунда секции main и выполняется функции написанные выше (makeBlack/makeWhite), в зависимости от того какое значение будет у условной переменной i, но я никак не смог понять как подстроить эти две функции по каждый бэкграунд(мне бы это было проще объяснить показав и озвучив), поэтому я хотел был разобраться в этом, не без божьей помощи конечно, а с Вашей)

Шрифты подключены в head с google fonts 

для иконок (search/cart/social) подключал fontawesome

Жду Вашего ответа!




